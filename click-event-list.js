const taskList = document.querySelector(`.list__text-${id}`);
const taskIds = taskList.split('-')[1];
const centerPanel = document.querySelector('.tasks__center-display');
const tasks =

taskList.addEventListener('click', (e) => {
    centerPanel.innHTML =
})
